{
  "name": "jazzkit",
  "private": true,
  "description": "MuseScore 4 plugins that nudge notation toward jazz conventions.",
  "scripts": {
    "test": "node test/run.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "check": "node scripts/check-qml.mjs JazzKit/*.qml JazzKit/lib/*.qml"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
