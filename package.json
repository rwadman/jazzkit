{
  "name": "jazzkit",
  "private": true,
  "description": "MuseScore 4 plugins that nudge notation toward jazz conventions.",
  "scripts": {
    "test": "node test/run.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "check": "node scripts/check-qml.mjs JazzKit/*.qml harness/*.qml",
    "e2e:check": "node scripts/check-acceptance.mjs",
    "verify": "npm test && npm run typecheck && npm run check && npm run e2e:check"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
